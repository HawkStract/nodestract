importa italiano da translate
importa * da nio
importa * da nnet

crea ris = richiedi("http://127.0.0.1:12345/retry")
stampa("Risultato Retry:", ris)

se (ris == "Retry success") {
    stampa("GET retry OK")
} altrimenti {
    stampa("FAIL: GET retry non ha restituito il valore previsto")
}
