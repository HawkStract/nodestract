use crate::engine::ast::Program;

pub struct Interpreter {}

impl Interpreter {
    pub fn new() -> Self {
        Self {}
    }

    /// Stub run method.
    pub fn run(&mut self, _program: Program) {
        // Bypassed for now.
    }
}